default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password:
  host: postgres

development:
  <<: *default
  database: hack2save-voluntariado_development

test:
  <<: *default
  database: hack2save-voluntariado_test

production:
  <<: *default
  database: hack2save-voluntariado_production
